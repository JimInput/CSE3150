#pragma once

template<class Container>
void bubble_sort(Container& con);

template<class Container>
void insertion_sort(Container& con);

template<class Container>
void selection_sort(Container& con);

template<class Container>
void quick_sort(Container& con);

template<class Container>
void merge_sort(Container& con);

template<class Container>
bool binary_search(Container& con, void* data);
