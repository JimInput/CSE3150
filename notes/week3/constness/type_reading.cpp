#include <iostream>

int main(void) {

    char *str[10]; // str is an array of 10 pointers to chars

    char *(*fp)(int, float *); // fp is a pointer to a function passing an int and float pointer returning a pointer to a char.

    return 0;
}
